name: ESPN Sync — Alle Turniere
on:
  schedule:
    - cron: '*/10 8-23 * * 5,6,7'
    - cron: '*/15 0-7,0-23 * * 1-4'
    - cron: '0 2 * * *'
  workflow_dispatch:
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Sync Scores + Fixtures
        env:
          SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          HTTP_CODE=$(curl -s -o /tmp/sync-result.json -w "%{http_code}" \
            -X POST "${SUPABASE_URL}/functions/v1/sync-match-results" \
            -H "Authorization: Bearer ${SERVICE_ROLE_KEY}" \
            -H "Content-Type: application/json")
          echo "HTTP ${HTTP_CODE}"
          python3 -m json.tool /tmp/sync-result.json 2>/dev/null | head -20
          [ "${HTTP_CODE}" = "200" ] || echo "::warning::Sync returned ${HTTP_CODE}"
