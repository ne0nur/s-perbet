# Archivierte Sync-Skripte

Diese Skripte wurden am 13.07.2026 archiviert, da sie **nicht aktiv** waren:
- `sync_loop.sh` — S4 Mini Termux Loop (8-Min-Takt, Platzhalter-Key, nie deployed)
- `sync_dynamic.sh` — Dynamischer Sync mit Edge-Function-gestertem Intervall (Platzhalter-Key, nie deployed)
- `sync_wm_smart.sh` — WM 2026 Optimierter Sync (Platzhalter-Key, nie deployed)

**Aktiver Sync:** GitHub Actions Cron (`sync-scores.yml`) triggert die Edge Function `sync-match-results`.
